body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(to bottom, black, purple);
  color: white;
  font-family: Arial, sans-serif;
  display: flex;
  align-items: center;
  justify-content: center;
}

#intro {
  font-size: 2em;
  animation: fadeOut 3s forwards;
  animation-delay: 3s;
}

@keyframes fadeOut {
  to { opacity: 0; visibility: hidden; }
}

#home {
  text-align: center;
}

.dock {
  position: fixed;
  bottom: 20px;
  width: 100%;
  display: flex;
  justify-content: center;
  gap: 20px;
}

.icon {
  width: 50px;
  transition: transform 0.2s;
  cursor: pointer;
}

.icon:hover {
  transform: scale(1.2) rotate(5deg);
}
#statusBar {
  position: fixed;
  top: 0;
  width: 100%;
  height: 30px;
  background: rgba(0,0,0,0.6);
  color: white;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 10px;
  font-size: 14px;
  z-index: 1000;
}

#controlCenter {
  position: fixed;
  top: 40px;
  right: 10px;
  width: 200px;
  background: rgba(0,0,0,0.9);
  color: white;
  padding: 10px;
  border-radius: 10px;
  z-index: 1001;
}

#controlCenter button {
  float: right;
  cursor: pointer;
  background: red;
  color: white;
  border: none;
  border-radius: 3px;
}

.controlItem {
  margin: 10px 0;
}
#carGameCanvas {
  display: none; /* show only when game opens */
  margin: 20px auto;
  background: #111;
  border-radius: 15px;
}
